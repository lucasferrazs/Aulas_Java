package execao;

public class _1oquesaoexecoes {
/*
 Exceções são eventos ou condições que interrompem o fluxo normal de um programa devido a erros inesperados. 
 Em Java, elas fazem parte do mecanismo de tratamento de erros e ajudam os desenvolvedores a lidar com situações 
 imprevistas durante a execução do código.

Tipos de exceções em Java:

Exceções verificadas: Precisam ser tratadas ou declaradas no código com throws. Exemplo: IOException.

Exceções não verificadas: Ocorrem durante a execução e não precisam ser declaradas. Exemplo: NullPointerException.

Erros: Representam problemas graves geralmente relacionados ao sistema, como falta de memória (OutOfMemoryError).

As exceções permitem capturar erros sem que o programa encerre abruptamente. Por exemplo, ao tentar abrir um arquivo inexistente, 
o Java pode lançar uma exceção (FileNotFoundException) que você pode tratar para informar o usuário e evitar falhas maiores. 

 */
}
